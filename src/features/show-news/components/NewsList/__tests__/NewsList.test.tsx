import { render, screen } from "@testing-library/react";
describe("NewsList", () => {
  it("should render the news based on its popularity", () => {});

  it("should show the number of news based on screen size", () => {});

  it("should render the headline", () => {});

  it("should show LoadingState when data is being loaded", () => {});

  it("should show ErrorState when there is an error", () => {});
});
